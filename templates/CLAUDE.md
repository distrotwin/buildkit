<!--
CLAUDE.md 骨架。面向在这个仓库里动手的人和 agent，与 README 分工：
README 讲怎么用镜像，本文件讲怎么改这个仓库。
必须覆盖的十项见 buildkit/docs/downstream-repo.md 第八节。

不要在本文件里绑定具体人名或邮箱。提交身份由本地 git 配置决定。
建好后执行 ln -sf CLAUDE.md AGENTS.md，确认 git ls-files -s AGENTS.md 是 mode 120000。
写完删掉本注释。
-->

# 开发指引

<一句话说清这个仓库是什么、定位边界，并指向 README 开头两节>

## 硬性约定

- commit **不允许带 co-author**
- 文档一律中文；Markdown **自然段内不换行**，一段写成一行长句
- 不在仓库里讨论许可与法务
- 写进文档的版本号一律来自跑镜像实测，不取源索引里的元包版本

## 这个仓库放什么

<目录树，以及「什么该进 buildkit、什么留在这里」的判据>

## 配置文件都有什么

<逐字段说明与改动注意，重点标出 IMAGE 必须唯一、EXPECT_* 不能留空、
按架构不同的基线必须条件覆写>

## 改动到跑通的完整回路

<两处 pin 的同步命令，以及打印两边实际取到的值来验证的那一步>

## 本地构建与验证

<每条构建路径一套完整命令>

## 跑 CI

<gh workflow run 的写法、两个开关的含义、改了 buildkit 之后不能 rerun --failed>

## 必知事实

<这个系统特有的、实测踩出来的结论。每条写清现象、真因，以及照着直觉改会走到哪个
错误方向。这是本文件最值钱的一节，只收实测结论，不收推断。>

## 门禁

| 门禁 | 防的是什么 |
|---|---|

<以及「加门禁要对门禁本身做双向变异测试」这条要求>

## 发布与验收

<匿名视角查 registry 的命令，以及要看的四件事>

## 排错

<这个项目里缺陷的典型形态，以及对应的排查纪律>

<!--
README 骨架。章节顺序不要动，每节要回答什么见 buildkit/docs/downstream-repo.md 第七节。

三条硬性要求：
  1. 每条命令、每段输出都必须实跑核对过，把代码块原样抽出来执行一遍
  2. 版本号一律来自跑镜像实测（gcc -dumpfullversion / ldd --version），不取源索引里的元包版本
  3. 中文，自然段内不换行；不写空洞的总结句与收尾金句
写完删掉本注释。
-->

# <系统中文名> · 构建与测试镜像

与真实<系统中文名>尽可能一致的容器环境，用于**软件构建、打包与兼容性测试**。<几个大版本、几个档位、跨哪些指令集>，全部从<厂商>官方<源类型>自举，公开在 GHCR。

```bash
docker run --rm ghcr.io/distrotwin/<镜像名>:<默认tag> \
  bash -c 'grep -E "^VERSION=" /etc/os-release; ldd --version | head -1; gcc -dumpfullversion'
```

```
<实跑输出，优先展示厂商写在 VERSION 里的中文系统名>
```

## 这是什么，不是什么

<镜像里没有内核、哪些内核态组件被顶替、systemd 保留到什么程度。>

<一条判据，说清它适合回答什么问题、不适合回答什么问题。>

**该用它**：<列举具体场景>。

**别用它**：<列举具体场景>。

## 先跑一遍

进容器，写个最小程序，编了跑。

```bash
docker run -it --rm ghcr.io/distrotwin/<镜像名>:<devel tag> /bin/bash
```

<进去以后的命令与实跑输出>

## 选哪一个

```
ghcr.io/distrotwin/<镜像名>:<版本>-<档位>
```

**先选版本**，按你要交付的目标机器：

| 版本 | 系统 | glibc | 编译器 | 上游血脉 | 架构 |
|---|---|---|---|---|---|

**再选档位**，按你要在里面做什么：

| 档位 | 装了什么 | 什么时候用 |
|---|---|---|
| `micro` | | 跑已经编好的二进制 |
| `base` | | 平台脚本、集成测试 |
| `devel` | | **编译与打包** |

<说明不带档位后缀的 tag 与 latest 各指向什么，附 docker pull 示例>

## 怎么用

<若干可直接粘贴的场景，每个都给命令与期望输出：查产物的 ABI 需求、多版本齐验、
在 GitHub Actions 里当构建环境、打包、跨架构运行、交互式排查>

## 认出自己在哪个系统上

```bash
docker run --rm ghcr.io/distrotwin/<镜像名>:<某个tag> cat /etc/os-release
```

<每个版本各贴一份原始输出，不要拼装脚本>

<脚本里判断平台时要留意的地方：VERSION_ID 是否能区分小版本、ID_LIKE 是否声明、
容器里 uname 报的是宿主内核>

## tag 与钉版

| tag | 性质 |
|---|---|

<日期 tag 取的是发布时仓库 HEAD 的 commit 日期；它锁不住上游源；要钉死用 digest>

## 镜像自带的溯源信息

| label | 内容 |
|---|---|

## 架构与 ABI 分叉

<同一版本号在不同架构下的差异；某些架构为什么没有镜像>

## 已知的怪癖与期望失败

<用户会撞见并误以为是 bug 的现象；XFAIL 项与它们在真机上同样成立的依据>

## 镜像是怎么造的

<走哪条构建路径、为什么、踩过哪些坑、验收强度与最近一轮的统计数字>

## 本地构建

<完整可执行的命令序列，每条构建路径一套>

## CI

<触发方式与两个开关；三阶段串行的说明>

## 仓库结构

<目录说明，以及指向 distrotwin/buildkit 的链接>
